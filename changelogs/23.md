Added rex-ask (one-shot text query) and rex-chat (persistent REPL) — both share the same memory DB, tools, and trust model as the voice daemon. Extracted shared LLM+tool pipeline into pipeline.py. Added [output] config section and format_tool_error() for actionable missing-dependency hints.
